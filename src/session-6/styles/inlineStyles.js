const MyComponent = () => {
    const myStyles = {
        color: 'blue',
        fontSize: '16px',
        // Add more styles as needed
    };

    return (
        <div style={myStyles}>
            Hello, World!
        </div>
    );
};
